pub mod generic_type_functions;
pub mod generic_type_structs;
pub mod generic_type_enums;

fn main() {
    println!("Hello, world!");
}

// We can use generic data types to act as astract stand ins
// for concrete types. Generic data types can represent any
// concrete type, and can hence be used to clear up duplicate 
// code.
// A generic type can have pre defined behaviour without needing to
// know its exact type. 

